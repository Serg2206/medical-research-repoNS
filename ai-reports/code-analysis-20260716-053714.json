[
  {
    "file": "main.py",
    "timestamp": "2026-07-16T05:35:35.355042",
    "metrics": {
      "complexity": "main.py\n    F 32:0 train_model - A (4)\n    F 72:0 predict - A (2)\n    C 14:0 MedicalNet - A (2)\n    F 92:0 main - A (1)\n    M 15:4 MedicalNet.__init__ - A (1)\n    M 22:4 MedicalNet.forward - A (1)\n",
      "maintainability": "main.py - A (74.92)\n"
    },
    "ai_recommendations": "В целом, код хорошо организован и легко читается. Однако, есть несколько моментов, которые можно улучшить:\n\n1. **Импорты**: В начале файла импортируется модуль `os`, который не используется в коде. Лучше убрать его, чтобы не замусоривать пространство имен.\n\n2. **Документация**: Ваш код будет легче понять и поддерживать, если вы добавите комментарии и строку документации (docstring) к каждому классу и функции. Это особенно важно для функций `train_model` и `predict`, где не совсем ясно, какие данные они принимают и что возвращают.\n\n3. **Обработка ошибок**: Ваш код не обрабатывает ошибки, которые могут возникнуть при работе с файлами. Например, при сохранении модели может возникнуть ошибка, если у вас нет прав на запись в текущую директорию. Лучше обернуть этот код в блок try/except.\n\n4. **Разделение обязанностей**: Функции `train_model` и `predict` выполняют слишком много задач. Лучше разделить их на более мелкие функции, каждая из которых выполняет одну задачу. Например, вы можете создать отдельные функции для создания модели, обучения модели, сохранения и загрузки модели.\n\n5. **Жестко закодированные параметры**: Параметры обучения, такие как количество эпох и размер батча, жестко закодированы в функции `train_model`. Лучше сделать их аргументами функции, чтобы вы могли легко изменять их при вызове функции.\n\n6. **Печать вместо возврата**: Функция `train_model` печатает потери на каждой эпохе, но не возвращает их. Лучше возвращать историю потерь в качестве списка, чтобы вы могли анализировать их после обучения.\n\n7. **Производительность**: Ваш код использует GPU, если он доступен, что хорошо для производительности. Однако, вы можете улучшить производительность еще больше, используя half precision (fp16) вместо full precision (fp32). Это может ускорить обучение и уменьшить использование памяти без значительной потери в качестве.\n\n8. **Безопасность**: Ваш код в целом безопасен, так как он не использует внешние данные и не выполняет никаких потенциально опасных операций. Однако, всегда стоит быть осторожным при работе с внешними данными и файлами.",
    "lines_of_code": 111
  },
  {
    "file": "prepare_data.py",
    "timestamp": "2026-07-16T05:35:52.278828",
    "metrics": {
      "complexity": "prepare_data.py\n    F 13:0 prepare_data - A (3)\n",
      "maintainability": "prepare_data.py - A (84.99)\n"
    },
    "ai_recommendations": "В целом, код хорошо структурирован и легко читается. Однако есть несколько моментов, которые можно улучшить:\n\n1. **Использование docstring**: Хорошо, что вы используете docstring для описания функции, но стоит следовать стандарту PEP 257. Вместо того чтобы начинать docstring с \"Подготовить данные:\", лучше написать \"Подготавливает данные...\". Также стоит добавить описание возвращаемых значений, даже если функция ничего не возвращает.\n\n2. **Обработка исключений**: В вашем коде есть проверка на наличие необходимых столбцов в данных, и при их отсутствии выбрасывается исключение. Это хорошо, но можно сделать еще лучше, добавив более подробное описание ошибки, чтобы было понятно, какие именно столбцы отсутствуют.\n\n3. **Очистка данных**: Вы очищаете данные, удаляя пробелы в начале и конце каждой строки. Это может быть не всегда необходимо и может замедлить обработку больших датасетов. Лучше сделать этот шаг опциональным.\n\n4. **Жестко заданный random_state**: В функции train_test_split вы используете жестко заданный random_state=42. Это делает разделение данных воспроизводимым, но в некоторых случаях может быть полезно иметь возможность изменять этот параметр. Рекомендуется сделать его аргументом функции с дефолтным значением.\n\n5. **Логирование**: Логирование важно для отслеживания прогресса и отладки. Однако, в вашем коде логируется только начало и конец обработки данных. Можно добавить больше информации, например, логировать размеры исходных данных, результаты очистки данных и т.д.\n\n6. **Использование argparse**: Ваш код использует argparse для обработки аргументов командной строки, что является best practice. Однако, вы могли бы добавить аргумент для test_size, чтобы пользователь мог контролировать размер тестовой выборки.\n\n7. **Сохранение данных**: Сейчас данные сохраняются в формате CSV без указания кодировки. Это может вызвать проблемы при работе с данными на разных платформах. Рекомендуется явно указывать кодировку при сохранении данных.",
    "lines_of_code": 53
  },
  {
    "file": "train.py",
    "timestamp": "2026-07-16T05:36:24.988676",
    "metrics": {
      "complexity": "train.py\n    F 77:0 train_model - A (4)\n    F 47:0 prepare_data - A (3)\n    C 22:0 MedicalDataset - A (2)\n    M 23:4 MedicalDataset.__init__ - A (1)\n    M 28:4 MedicalDataset.__len__ - A (1)\n    M 31:4 MedicalDataset.__getitem__ - A (1)\n",
      "maintainability": "train.py - A (69.54)\n"
    },
    "ai_recommendations": "В целом, код хорошо структурирован и легко читаем. Однако есть несколько рекомендаций по улучшению:\n\n1. **Обработка ошибок и валидация входных данных**: В функции `prepare_data` есть проверка на наличие необходимых столбцов в данных, что хорошо. Однако, можно добавить больше проверок, например, проверить, что файл существует перед его чтением, проверить, что директория для сохранения данных существует и доступна для записи, и т.д.\n\n2. **Использование глобальных переменных**: В коде используются глобальные переменные `MODEL_NAME` и `DEVICE`. Это может привести к проблемам, если код будет расширяться или использоваться в большом проекте. Лучше передавать эти значения как аргументы функций или методы классов.\n\n3. **Жестко закодированные параметры**: Параметры, такие как размер батча, количество эпох, learning rate, и т.д., жестко закодированы в функции `train_model`. Это делает код менее гибким и переиспользуемым. Лучше передавать эти значения как аргументы функции.\n\n4. **Чтение данных из файла в функции `train_model`**: Это может быть неэффективно, если данные большие. Лучше передавать уже загруженные данные в функцию.\n\n5. **Отсутствие комментариев**: Хотя код в целом хорошо структурирован и легко читаем, некоторые части могут быть не совсем понятны для новых разработчиков или людей, не знакомых с библиотеками, которые вы используете. Хорошей практикой является добавление комментариев к сложным или важным частям кода.\n\n6. **Отсутствие тестов**: В коде нет тестов. Тесты помогают обнаружить ошибки и убедиться, что код работает правильно при изменении или расширении. Рекомендуется добавить unit-тесты и, возможно, интеграционные тесты.\n\n7. **Отсутствие логирования в процессе обучения**: В процессе обучения модели логируется только потеря после каждой эпохи. Для более детального анализа процесса обучения может быть полезно логировать также и другие метрики, например, точность на валидационной выборке после каждой эпохи.",
    "lines_of_code": 124
  },
  {
    "file": "infer.py",
    "timestamp": "2026-07-16T05:36:57.646656",
    "metrics": {
      "complexity": "infer.py\n    F 10:0 infer_model - A (2)\n",
      "maintainability": "infer.py - A (74.30)\n"
    },
    "ai_recommendations": "В целом, ваш код выглядит хорошо. Однако есть некоторые моменты, которые можно улучшить:\n\n1. **Производительность**: Ваша функция `infer_model` каждый раз загружает модель и токенизатор при каждом вызове. Если вы планируете вызывать эту функцию несколько раз, это может быть неэффективно. Рассмотрите возможность загрузки модели и токенизатора один раз и передачи их в функцию в качестве аргументов.\n\n2. **Читаемость**: Ваш код в целом хорошо структурирован и легко читаем. Однако, комментарии могут помочь улучшить понимание того, что делает ваш код, особенно для людей, не знакомых с библиотекой transformers.\n\n3. **Best Practices**: Вместо использования глобальной переменной `DEVICE`, рассмотрите возможность передачи ее в качестве аргумента в функцию `infer_model`. Это сделает вашу функцию более гибкой и позволит ей работать в разных контекстах.\n\n4. **Безопасность**: Ваш код не содержит явных проблем с безопасностью. Однако, убедитесь, что входные данные проверяются на наличие возможных атак, таких как инъекции кода, особенно если этот код будет использоваться в веб-приложении.\n\nВот пример того, как можно было бы улучшить ваш код:\n\n```python\nimport argparse\nimport torch\nfrom transformers import AutoModelForSequenceClassification, AutoTokenizer\n\ndef load_model_and_tokenizer(model_dir, device):\n    tokenizer = AutoTokenizer.from_pretrained(model_dir)\n    model = AutoModelForSequenceClassification.from_pretrained(model_dir)\n    model.to(device)\n    return model, tokenizer\n\ndef infer_model(model, tokenizer, text, device):\n    tokens = tokenizer(\n        text, return_tensors=\"pt\", truncation=True, padding=True, max_length=128\n    )\n    tokens = {k: v.to(device) for k, v in tokens.items()}\n\n    model.eval()\n    with torch.no_grad():\n        outputs = model(**tokens)\n        probabilities = torch.softmax(outputs.logits, dim=-1)\n\n    return probabilities.tolist()\n\nif __name__ == \"__main__\":\n    parser = argparse.ArgumentParser(description=\"Инференс с медицинской нейросетью\")\n    parser.add_argument(\"--model\", required=True, help=\"Путь к сохраненной модели\")\n    parser.add_argument(\"--text\", required=True, help=\"Текст для анализа\")\n    args = parser.parse_args()\n\n    device = torch.device(\"cuda\" if torch.cuda.is_available() else \"cpu\")\n    model, tokenizer = load_model_and_tokenizer(args.model, device)\n    result = infer_model(model, tokenizer, args.text, device)\n    print(f\"Результаты предсказания: {result}\")\n```\nЭтот код делает то же самое, что и ваш, но он загружает модель и токенизатор один раз, а затем передает их в функцию `infer_model`. Это может улучшить производительность, если функция `infer_model` вызывается несколько раз.",
    "lines_of_code": 35
  },
  {
    "file": "scripts/fix_csv_headers.py",
    "timestamp": "2026-07-16T05:37:14.109807",
    "metrics": {
      "complexity": "",
      "maintainability": "scripts/fix_csv_headers.py - A (90.78)\n"
    },
    "ai_recommendations": "В целом, код написан хорошо, но есть несколько моментов, которые можно улучшить:\n\n1. **Производительность**: В данном случае, весь файл загружается в память сразу, что может быть проблематично для больших файлов. Вместо этого, можно использовать генераторы или итераторы, чтобы обрабатывать файл построчно.\n\n2. **Безопасность**: Скрипт перезаписывает исходный файл, что может привести к потере данных. Лучше сохранять результаты в новый файл.\n\n3. **Читаемость**: Строки кода, которые обрабатывают валидные и невалидные строки, можно объединить в одну, чтобы улучшить читаемость.\n\n4. **Best Practices**: Хорошей практикой является использование функций для обработки логики, что упрощает тестирование и повторное использование кода.\n\nВот пример улучшенного кода:\n\n```python\n#!/usr/bin/env python3\n\"\"\"Fix CSV file by removing invalid header rows\"\"\"\n\nimport csv\nimport sys\n\ndef filter_invalid_lines(file_path):\n    \"\"\"Filter out lines that don't have proper CSV structure\"\"\"\n    valid_lines = []\n    with open(file_path, \"r\", encoding=\"utf-8\") as f:\n        for i, line in enumerate(f):\n            if i == 0 or \",\" in line:  # Keep the header row and lines with CSV data\n                valid_lines.append(line)\n            else:\n                print(f\"Removing invalid line {i+1}: {line.strip()}\")\n    return valid_lines\n\ndef write_cleaned_csv(file_path, valid_lines):\n    \"\"\"Write back the cleaned CSV to a new file\"\"\"\n    with open(\"cleaned_\" + file_path, \"w\", encoding=\"utf-8\") as f:\n        f.writelines(valid_lines)\n\ndef main():\n    file_path = \"training-data/medical_training_data.csv\"\n    valid_lines = filter_invalid_lines(file_path)\n    write_cleaned_csv(file_path, valid_lines)\n    print(f\"\\nDone! Kept {len(valid_lines)} valid lines, removed {len(lines) - len(valid_lines)} invalid lines\")\n\nif __name__ == \"__main__\":\n    main()\n```\n\nВ этом коде функции `filter_invalid_lines` и `write_cleaned_csv` обрабатывают чтение и запись файлов, а функция `main` объединяет эти операции. Также результаты сохраняются в новый файл, чтобы избежать перезаписи исходного файла.",
    "lines_of_code": 29
  }
]